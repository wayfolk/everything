###### relies on `.wslconfig` from `./config/` to be present in `%UserProfile%`

let's create our single instance

cd .\_provisioning/
.\provision.ps1

and to clean up

cd .\_provisioning/
.\cleanup.ps1